{
  "name": "rosuzet-dyslipidemia-study-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist/assets && cp index.html *.css *.js dist/ && cp assets/*.svg dist/assets/",
    "test": "node --test tests/*.test.mjs",
    "test:browser": "node tests/browser-smoke.mjs"
  }
}
